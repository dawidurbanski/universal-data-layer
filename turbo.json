{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^lint:types", "^lint", "^build"],
      "outputs": ["dist/**"]
    },
    "lint:types": {
      "dependsOn": ["^lint:types"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "dev": {
      "persistent": true,
      "cache": false
    },
    "start": {
      "dependsOn": ["^start"]
    }
  }
}
