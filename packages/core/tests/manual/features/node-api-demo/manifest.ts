import type { ScenarioManifest } from '../../../../../../tests/manual/src/types';

const manifest: ScenarioManifest = {
  package: 'core',
  feature: 'node-api-demo',
  title: 'Node API Demo',
  description:
    'Demonstrates the Node API with three plugins: data sourcing, node extension, and node filtering',
  dependsOn: [],
};

export default manifest;
