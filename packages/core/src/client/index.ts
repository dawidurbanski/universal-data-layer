/**
 * Client-side utilities for Universal Data Layer.
 *
 * These are lightweight helpers designed for browser/client environments.
 */

export { resolveRefs, type NormalizedResponse } from './resolveRefs.js';
